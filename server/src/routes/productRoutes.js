import express from"express";import upload from"../middleware/upload.js";import{getProducts,createProduct,updateProduct,deleteProduct,updateStatus}from"../controllers/productController.js";const router=express.Router();router.route("/").get(getProducts).post(upload.array("images",5),createProduct);router.patch("/:id/status",updateStatus);router.route("/:id").put(upload.array("images",5),updateProduct).delete(deleteProduct);export default router;