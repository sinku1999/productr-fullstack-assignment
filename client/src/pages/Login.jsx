import{useState}from"react";import{useNavigate}from"react-router-dom";import toast from"react-hot-toast";import AuthVisual from"../components/AuthVisual.jsx";import api from"../services/api.js";export default function Login(){const[identifier,setIdentifier]=useState("");const navigate=useNavigate();const submit=async e=>{e.preventDefault();if(!identifier.trim())return toast.error("Enter email or phone number");try{await api.post("/auth/login",{identifier});localStorage.setItem("productr_identifier",identifier);toast.success("OTP sent successfully");navigate("/verify-otp")}catch{toast.error("Start backend first")}};return <main className="auth-page"><div className="auth-frame"><AuthVisual/><section className="auth-panel"><form onSubmit={submit} className="login-form"><h1>Login to your Productr Account</h1><input value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder="Enter email or phone number"/><button>Login</button></form><div className="signup-box"><p>Don’t have a Productr Account</p><b>SignUp Here</b></div></section></div></main>}