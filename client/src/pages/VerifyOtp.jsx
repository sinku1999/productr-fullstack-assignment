import{useState}from"react";import{useNavigate}from"react-router-dom";import toast from"react-hot-toast";import AuthVisual from"../components/AuthVisual.jsx";import OtpInput from"../components/OtpInput.jsx";import api from"../services/api.js";export default function VerifyOtp(){const[otp,setOtp]=useState("");const[error,setError]=useState(false);const navigate=useNavigate();const submit=async e=>{e.preventDefault();setError(false);try{const identifier=localStorage.getItem("productr_identifier");const res=await api.post("/auth/verify-otp",{identifier,otp});localStorage.setItem("productr_token",res.data.token);toast.success("Login successful");navigate("/dashboard")}catch{setError(true);toast.error("Invalid OTP. Use 123456")}};return <main className="auth-page"><div className="auth-frame"><AuthVisual/><section className="auth-panel"><form onSubmit={submit} className="login-form otp-form"><h1>Login to your Productr Account</h1><label>Enter OTP</label><OtpInput value={otp} onChange={setOtp} error={error}/><button>Enter your OTP</button><p className="resend-text">Didnt receive OTP ? <b>Resend</b></p></form></section></div></main>}