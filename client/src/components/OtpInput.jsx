export default function OtpInput({value,onChange,error}){const arr=value.padEnd(6," ").slice(0,6).split("");const setDigit=(i,d)=>{if(!/^\d?$/.test(d))return;const n=arr.map(x=>x===" "?"":x);n[i]=d;onChange(n.join("").slice(0,6));if(d){const el=document.getElementById(`otp-${i+1}`);if(el)el.focus()}};return <><div className="otp-row">{arr.map((d,i)=><input id={`otp-${i}`} key={i} value={d===" "?"":d} maxLength={1} onChange={e=>setDigit(i,e.target.value)} className={error?"otp-input error":"otp-input"}/>)}</div>{error&&<p className="field-error">Please enter a valid OTP</p>}</>}